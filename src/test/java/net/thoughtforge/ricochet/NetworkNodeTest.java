package net.thoughtforge.ricochet;

import net.thoughtforge.commons.logging.Log;
import net.thoughtforge.commons.logging.LogLevel;

import org.junit.Test;

public class NetworkNodeTest extends AbstractProtocolTest {

	/**
	 * A network of 3 nodes is established and the state of nodeA, nodeB and nodeC is then verified.
	 */
	@Log(logLevel=LogLevel.DEBUG)
	@Test
	public void establishNetwork() throws Exception {
		
	}
}
